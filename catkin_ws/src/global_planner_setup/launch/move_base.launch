<launch>
  <param name="use_sim_time" value="true"/>

  <!-- Load global costmap -->
  <rosparam file="$(find global_planner_setup)/config/global_costmap_params.yaml"
            command="load"/>

  <!-- Load local costmap -->
  <rosparam file="$(find global_planner_setup)/config/local_costmap_params.yaml"
            command="load"/>

  <!-- Start move_base -->
  <node pkg="move_base" type="move_base" name="move_base" output="screen">
    <param name="base_global_planner" value="navfn/NavfnROS"/>
    <param name="base_local_planner" value="base_local_planner/TrajectoryPlannerROS"/>
  </node>
</launch>
